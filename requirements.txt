scipy==1.13.1
scikit-learn==1.3.0
numpy==1.26.4
meshio==5.3.4
matplotlib==3.9.2

# scikit-geometry 0.1.2 is required by data_myMeshes.py through
# "import skgeom as sg", but it is best installed from conda-forge.
# Do not combine skgeom 0.1.2 with numpy>=2: the straight-skeleton
# binding can fail with a pybind11 argument-conversion error.
#
# Recommended:
#   conda env create -f environment.yml
#
# If you install scikit-geometry manually, run:
#   python tests/smoke_skgeom.py
